# ctrld-prom-exporter

```bash
CTRLD_API_TOKEN=api.1234321 CTRLD_EXPORTER_PORT=11000 CTRLD_EXPORTER_LOGS=true npm run start
# OR
npm run start --token=api.1234321 --port=11000 --logs

# only token required
# default port: 11009
# logs var enables endpoint requests console logging
```
